class Group:

	def __init__(self, groupname, hostname):
		self.groupname = groupname
		self.groupID = None # we need to find a way to hash new Ids or something

		self.users = [hostname]




	def create_group_playlist(self, groupname):
		# create a group playlist

	def add_member(self, userId):

	def remove_member(self, userId):

	def refresh_group_playlist(self):
		# the weekly update

	